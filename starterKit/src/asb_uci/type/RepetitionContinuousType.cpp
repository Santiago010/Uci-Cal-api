/** @file RepetitionContinuousType.cpp
 *
 * @warning This file is automatically generated, edit at your own risk
 *
 * This file was generated on 04/01/2024 at 02:25:14 PM
 *
 * <pre>
 * Unclassified               U N C L A S S I F I E D               Unclassified
 *
 * DISTRIBUTION STATEMENT D: Distribution authorized to Department of Defense
 *    and U.S. DoD contractors only, Critical Technology, Export Controlled,
 *    26 January 2023. Other requests for this document shall be referred to
 *    AFLCMC/XA (OAMO), Wright-Patterson AFB OH 45433.
 *
 * DESTRUCTION NOTICE: Destroy by any method that will prevent disclosure of
 *    contents or reconstruction of the document.
 *
 * WARNING - This document contains technical data whose export is restricted
 *    by the Arms Export Control Act (Section 2751 of Title 22, United States
 *    Code) or the Export Control Reform Act of 2018 (Chapter 58 Sections
 *    4801-4852 of Title 50, United States Code). Violations of these export
 *    laws are subject to severe criminal penalties. Disseminate in accordance
 *    with provisions of DoD Directive 5230.25 and DoD Instruction 2040.02.
 *
 * Controlled by: USAF
 * Controlled by: AFLCMC/XA (OAMO)
 * CUI Category(ies): CTI, EXPT
 * Distribution Statement D
 * POC: Wayne Artuso, 937-713-5124
 *
 * CUI//SP-CTI/SP-EXPT
 * </pre>
 *
 */

#include "asb_uci/type/RepetitionContinuousType.h"

#include <memory>
#include <string>

#include <boost/optional/optional.hpp>
#include <boost/smart_ptr/make_unique.hpp>

#include "asb_uci/util/SerializationHelpers.h"
#include "uci/base/UCIException.h"
#include "uci/base/accessorType.h"
#include "uci/type/DurationType.h"
#include "uci/type/RepetitionContinuousType.h"

/**  */
namespace asb_uci {

/** The namespace in which all generated data types are declared */
namespace type {

RepetitionContinuousType::RepetitionContinuousType() = default;

RepetitionContinuousType::~RepetitionContinuousType() = default;

void RepetitionContinuousType::copy(const uci::type::RepetitionContinuousType& accessor) {
  copyImpl(accessor, false);
}

void RepetitionContinuousType::copyImpl(const uci::type::RepetitionContinuousType& accessor, const bool /*checkIfDerivation*/) {
  if (&accessor != this) {
    const auto& accessorImpl = dynamic_cast<const RepetitionContinuousType&>(accessor);
    if (accessorImpl.hasMaximumInterruptDuration()) {
      setMaximumInterruptDuration(accessorImpl.getMaximumInterruptDuration());
    } else {
      clearMaximumInterruptDuration();
    }
  }
}

void RepetitionContinuousType::reset() noexcept {
  clearMaximumInterruptDuration();
}

uci::type::DurationTypeValue RepetitionContinuousType::getMaximumInterruptDuration() const {
  if (maximumInterruptDuration_Accessor) {
    return *maximumInterruptDuration_Accessor;
  }
  throw uci::base::UCIException("Error in getMaximumInterruptDuration(): An attempt was made to get an optional field that was not enabled, call hasMaximumInterruptDuration() to determine if it is safe to call getMaximumInterruptDuration()");
}

uci::type::RepetitionContinuousType& RepetitionContinuousType::setMaximumInterruptDuration(uci::type::DurationTypeValue value) {
  maximumInterruptDuration_Accessor = value;
  return *this;
}

bool RepetitionContinuousType::hasMaximumInterruptDuration() const noexcept {
  return maximumInterruptDuration_Accessor.has_value();
}

uci::type::RepetitionContinuousType& RepetitionContinuousType::clearMaximumInterruptDuration() noexcept {
  maximumInterruptDuration_Accessor.reset();
  return *this;
}

std::unique_ptr<RepetitionContinuousType> RepetitionContinuousType::create(const uci::base::accessorType::AccessorType type) {
  const uci::base::accessorType::AccessorType requestedType{(type == uci::base::accessorType::null) ? uci::type::accessorType::repetitionContinuousType : type};
  return (requestedType == uci::type::accessorType::repetitionContinuousType) ? boost::make_unique<RepetitionContinuousType>() : nullptr;
}

/**  */
namespace RepetitionContinuousType_Names {

constexpr const char* Extern_Type_Name{"RepetitionContinuousType"};
constexpr const char* MaximumInterruptDuration_Name{"MaximumInterruptDuration"};

} // namespace RepetitionContinuousType_Names

void RepetitionContinuousType::deserialize(const boost::property_tree::ptree& propTree, uci::type::RepetitionContinuousType& accessor, std::string& nodeName, std::string& nsPrefix, const bool topLevel) {
  nodeName = RepetitionContinuousType_Names::Extern_Type_Name;
  const boost::property_tree::ptree& node = (topLevel ? propTree.ordered_begin()->second : propTree);
  for (const boost::property_tree::ptree::value_type& valueType : node.get_child("")) {
    if ((valueType.first == "<xmlattr>") && topLevel) {
      nsPrefix = asb_uci::util::SerializationHelpers::checkForNamespacePrefix(valueType.second.get_child(""));
    } else if (valueType.first == nsPrefix + RepetitionContinuousType_Names::MaximumInterruptDuration_Name) {
      if (const boost::optional<std::string> value = valueType.second.get_value_optional<std::string>()) {
        accessor.setMaximumInterruptDuration(asb_uci::util::SerializationHelpers::deserializeDuration(*value));
      }
    }
  }
}

std::string RepetitionContinuousType::serialize(const uci::type::RepetitionContinuousType& accessor, boost::property_tree::ptree& propTree, const std::string& nodeName, const bool createNode, const bool addTypeAttribute, const bool /*checkIfDerivation*/, const bool /*topLevel*/) {
  std::string generatedNodeName{(nodeName.empty() ? RepetitionContinuousType_Names::Extern_Type_Name : nodeName)};
  boost::property_tree::ptree newNode;
  boost::property_tree::ptree& node = (createNode ? newNode : propTree);
  if (addTypeAttribute) {
    asb_uci::util::SerializationHelpers::addTypeAttribute(node, RepetitionContinuousType_Names::Extern_Type_Name);
  }
  if (accessor.hasMaximumInterruptDuration()) {
    asb_uci::util::SerializationHelpers::serializeDuration(accessor.getMaximumInterruptDuration(), node, RepetitionContinuousType_Names::MaximumInterruptDuration_Name);
  }
  if (createNode) {
    propTree.add_child(generatedNodeName, node);
  }
  return generatedNodeName;
}

} // namespace type

} // namespace asb_uci

/** The namespace in which all UCI data types are declared */
namespace uci {

/** The namespace in which all generated data types are declared */
namespace type {

uci::type::RepetitionContinuousType& RepetitionContinuousType::create(uci::base::AbstractServiceBusConnection* /*abstractServiceBusConnection*/) {
  return *(boost::make_unique<asb_uci::type::RepetitionContinuousType>().release());
}

uci::type::RepetitionContinuousType& RepetitionContinuousType::create(const uci::type::RepetitionContinuousType& accessor, uci::base::AbstractServiceBusConnection* /*abstractServiceBusConnection*/) {
  std::unique_ptr<asb_uci::type::RepetitionContinuousType> newAccessor{boost::make_unique<asb_uci::type::RepetitionContinuousType>()};
  newAccessor->copyImpl(accessor, false);
  return *(newAccessor.release());
}

void RepetitionContinuousType::destroy(uci::type::RepetitionContinuousType& accessor) {
  delete dynamic_cast<asb_uci::type::RepetitionContinuousType*>(&accessor);
}

} // namespace type

} // namespace uci


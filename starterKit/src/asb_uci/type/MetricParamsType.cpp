/** @file MetricParamsType.cpp
 *
 * @warning This file is automatically generated, edit at your own risk
 *
 * This file was generated on 04/01/2024 at 02:25:13 PM
 *
 * <pre>
 * Unclassified               U N C L A S S I F I E D               Unclassified
 *
 * DISTRIBUTION STATEMENT D: Distribution authorized to Department of Defense
 *    and U.S. DoD contractors only, Critical Technology, Export Controlled,
 *    26 January 2023. Other requests for this document shall be referred to
 *    AFLCMC/XA (OAMO), Wright-Patterson AFB OH 45433.
 *
 * DESTRUCTION NOTICE: Destroy by any method that will prevent disclosure of
 *    contents or reconstruction of the document.
 *
 * WARNING - This document contains technical data whose export is restricted
 *    by the Arms Export Control Act (Section 2751 of Title 22, United States
 *    Code) or the Export Control Reform Act of 2018 (Chapter 58 Sections
 *    4801-4852 of Title 50, United States Code). Violations of these export
 *    laws are subject to severe criminal penalties. Disseminate in accordance
 *    with provisions of DoD Directive 5230.25 and DoD Instruction 2040.02.
 *
 * Controlled by: USAF
 * Controlled by: AFLCMC/XA (OAMO)
 * CUI Category(ies): CTI, EXPT
 * Distribution Statement D
 * POC: Wayne Artuso, 937-713-5124
 *
 * CUI//SP-CTI/SP-EXPT
 * </pre>
 *
 */

#include "asb_uci/type/MetricParamsType.h"

#include <memory>
#include <string>

#include <boost/optional/optional.hpp>
#include <boost/smart_ptr/make_unique.hpp>

#include "asb_uci/util/SerializationHelpers.h"
#include "uci/base/UCIException.h"
#include "uci/base/accessorType.h"
#include "uci/type/DistanceType.h"
#include "uci/type/DurationType.h"
#include "uci/type/MetricParamsType.h"

/**  */
namespace asb_uci {

/** The namespace in which all generated data types are declared */
namespace type {

MetricParamsType::MetricParamsType() = default;

MetricParamsType::~MetricParamsType() = default;

void MetricParamsType::copy(const uci::type::MetricParamsType& accessor) {
  copyImpl(accessor, false);
}

void MetricParamsType::copyImpl(const uci::type::MetricParamsType& accessor, const bool /*checkIfDerivation*/) {
  if (&accessor != this) {
    const auto& accessorImpl = dynamic_cast<const MetricParamsType&>(accessor);
    if (accessorImpl.hasPosMetricResolution()) {
      setPosMetricResolution(accessorImpl.getPosMetricResolution());
    } else {
      clearPosMetricResolution();
    }
    if (accessorImpl.hasProductResolutionTime()) {
      setProductResolutionTime(accessorImpl.getProductResolutionTime());
    } else {
      clearProductResolutionTime();
    }
  }
}

void MetricParamsType::reset() noexcept {
  clearPosMetricResolution();
  clearProductResolutionTime();
}

uci::type::DistanceTypeValue MetricParamsType::getPosMetricResolution() const {
  if (posMetricResolution_Accessor) {
    return *posMetricResolution_Accessor;
  }
  throw uci::base::UCIException("Error in getPosMetricResolution(): An attempt was made to get an optional field that was not enabled, call hasPosMetricResolution() to determine if it is safe to call getPosMetricResolution()");
}

uci::type::MetricParamsType& MetricParamsType::setPosMetricResolution(uci::type::DistanceTypeValue value) {
  posMetricResolution_Accessor = value;
  return *this;
}

bool MetricParamsType::hasPosMetricResolution() const noexcept {
  return posMetricResolution_Accessor.has_value();
}

uci::type::MetricParamsType& MetricParamsType::clearPosMetricResolution() noexcept {
  posMetricResolution_Accessor.reset();
  return *this;
}

uci::type::DurationTypeValue MetricParamsType::getProductResolutionTime() const {
  if (productResolutionTime_Accessor) {
    return *productResolutionTime_Accessor;
  }
  throw uci::base::UCIException("Error in getProductResolutionTime(): An attempt was made to get an optional field that was not enabled, call hasProductResolutionTime() to determine if it is safe to call getProductResolutionTime()");
}

uci::type::MetricParamsType& MetricParamsType::setProductResolutionTime(uci::type::DurationTypeValue value) {
  productResolutionTime_Accessor = value;
  return *this;
}

bool MetricParamsType::hasProductResolutionTime() const noexcept {
  return productResolutionTime_Accessor.has_value();
}

uci::type::MetricParamsType& MetricParamsType::clearProductResolutionTime() noexcept {
  productResolutionTime_Accessor.reset();
  return *this;
}

std::unique_ptr<MetricParamsType> MetricParamsType::create(const uci::base::accessorType::AccessorType type) {
  const uci::base::accessorType::AccessorType requestedType{(type == uci::base::accessorType::null) ? uci::type::accessorType::metricParamsType : type};
  return (requestedType == uci::type::accessorType::metricParamsType) ? boost::make_unique<MetricParamsType>() : nullptr;
}

/**  */
namespace MetricParamsType_Names {

constexpr const char* Extern_Type_Name{"MetricParamsType"};
constexpr const char* PosMetricResolution_Name{"PosMetricResolution"};
constexpr const char* ProductResolutionTime_Name{"ProductResolutionTime"};

} // namespace MetricParamsType_Names

void MetricParamsType::deserialize(const boost::property_tree::ptree& propTree, uci::type::MetricParamsType& accessor, std::string& nodeName, std::string& nsPrefix, const bool topLevel) {
  nodeName = MetricParamsType_Names::Extern_Type_Name;
  const boost::property_tree::ptree& node = (topLevel ? propTree.ordered_begin()->second : propTree);
  for (const boost::property_tree::ptree::value_type& valueType : node.get_child("")) {
    if ((valueType.first == "<xmlattr>") && topLevel) {
      nsPrefix = asb_uci::util::SerializationHelpers::checkForNamespacePrefix(valueType.second.get_child(""));
    } else if (valueType.first == nsPrefix + MetricParamsType_Names::PosMetricResolution_Name) {
      if (const boost::optional<std::string> value = valueType.second.get_value_optional<std::string>()) {
        accessor.setPosMetricResolution(asb_uci::util::SerializationHelpers::deserializeDouble(*value));
      }
    } else if (valueType.first == nsPrefix + MetricParamsType_Names::ProductResolutionTime_Name) {
      if (const boost::optional<std::string> value = valueType.second.get_value_optional<std::string>()) {
        accessor.setProductResolutionTime(asb_uci::util::SerializationHelpers::deserializeDuration(*value));
      }
    }
  }
}

std::string MetricParamsType::serialize(const uci::type::MetricParamsType& accessor, boost::property_tree::ptree& propTree, const std::string& nodeName, const bool createNode, const bool addTypeAttribute, const bool /*checkIfDerivation*/, const bool /*topLevel*/) {
  std::string generatedNodeName{(nodeName.empty() ? MetricParamsType_Names::Extern_Type_Name : nodeName)};
  boost::property_tree::ptree newNode;
  boost::property_tree::ptree& node = (createNode ? newNode : propTree);
  if (addTypeAttribute) {
    asb_uci::util::SerializationHelpers::addTypeAttribute(node, MetricParamsType_Names::Extern_Type_Name);
  }
  if (accessor.hasPosMetricResolution()) {
    asb_uci::util::SerializationHelpers::serializeDouble(accessor.getPosMetricResolution(), node, MetricParamsType_Names::PosMetricResolution_Name);
  }
  if (accessor.hasProductResolutionTime()) {
    asb_uci::util::SerializationHelpers::serializeDuration(accessor.getProductResolutionTime(), node, MetricParamsType_Names::ProductResolutionTime_Name);
  }
  if (createNode) {
    propTree.add_child(generatedNodeName, node);
  }
  return generatedNodeName;
}

} // namespace type

} // namespace asb_uci

/** The namespace in which all UCI data types are declared */
namespace uci {

/** The namespace in which all generated data types are declared */
namespace type {

uci::type::MetricParamsType& MetricParamsType::create(uci::base::AbstractServiceBusConnection* /*abstractServiceBusConnection*/) {
  return *(boost::make_unique<asb_uci::type::MetricParamsType>().release());
}

uci::type::MetricParamsType& MetricParamsType::create(const uci::type::MetricParamsType& accessor, uci::base::AbstractServiceBusConnection* /*abstractServiceBusConnection*/) {
  std::unique_ptr<asb_uci::type::MetricParamsType> newAccessor{boost::make_unique<asb_uci::type::MetricParamsType>()};
  newAccessor->copyImpl(accessor, false);
  return *(newAccessor.release());
}

void MetricParamsType::destroy(uci::type::MetricParamsType& accessor) {
  delete dynamic_cast<asb_uci::type::MetricParamsType*>(&accessor);
}

} // namespace type

} // namespace uci

